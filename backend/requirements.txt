# Sovereign AI — requirements.txt
# Target environment: Conda + Python 3.11.9
# Platform: Windows development
#
# IMPORTANT:
# PaddlePaddle/PaddleOCR are installed separately because the
# Windows PaddlePaddle 2.6.2 stack has a protobuf constraint that
# conflicts with the grpcio-tools dependency pulled by qdrant-client.
#
# GPU runtimes are also installed separately:
# - PyTorch/CUDA
# - Qwen GGUF / llama.cpp
# - vLLM (Linux/WSL2 only)

# -----------------------------
# API / Backend
# -----------------------------
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
pydantic-settings==2.5.2
python-multipart==0.0.12
aiofiles==24.1.0
python-dotenv==1.0.1
httpx==0.27.2
sse-starlette==2.1.0

# -----------------------------
# Agent / LangChain
# -----------------------------
langgraph==0.2.50
langchain-core==0.3.22
langchain-community==0.3.10
langchain-openai==0.2.9
openai==1.55.3

# -----------------------------
# Local model serving
# -----------------------------
# vLLM is Linux/WSL2 only.
vllm==0.6.6; platform_system != "Windows"

# Qwen2.5-Coder-3B-Instruct GGUF (local LLM runtime)
# On Linux/WSL2 this installs normally from PyPI.
# On Windows the source build fails (path-too-long), so install the
# prebuilt CPU wheel manually instead:
#   pip install https://github.com/abetlen/llama-cpp-python/releases/download/v0.3.35/llama_cpp_python-0.3.35-py3-none-win_amd64.whl
# Pulls in diskcache/jinja2/numpy; all compatible with the pins below.
llama-cpp-python==0.3.35; platform_system != "Windows"

# -----------------------------
# Retrieval / Vector DB
# -----------------------------
qdrant-client==1.12.1
bm25s==0.2.3
sentence-transformers==3.2.1

# -----------------------------
# Database
# -----------------------------
psycopg2-binary==2.9.10
sqlalchemy==2.0.35
asyncpg==0.29.0

# -----------------------------
# Documents
# -----------------------------
pymupdf==1.25.2

# -----------------------------
# Office / Email artifacts
# -----------------------------
python-docx==1.1.2
openpyxl==3.1.5
python-pptx==1.0.1
extract-msg==0.53.1

# -----------------------------
# Numerical / scientific
# -----------------------------
numpy==1.26.4
scipy==1.14.1

# -----------------------------
# Development / Testing
# -----------------------------
pytest==8.3.3
pytest-asyncio==0.24.0
pytest-cov==5.0.0
ruff==0.6.9
mypy==1.11.2

# Pylance is a VS Code extension,
# not a Python package.